============
Installation
============

At the command line::

    $ easy_install django-curriculum

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-curriculum
    $ pip install django-curriculum
